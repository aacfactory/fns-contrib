package internal

import (
	"context"
	"github.com/aacfactory/errors"
)

func keys(ctx context.Context, client Client, key string) (err errors.CodeError) {

	return
}

func remove(ctx context.Context, client Client, key string) (err errors.CodeError) {

	return
}

func exists(ctx context.Context, client Client, key string) (err errors.CodeError) {

	return
}

func expire(ctx context.Context, client Client, key string) (err errors.CodeError) {

	return
}

func persist(ctx context.Context, client Client, key string) (err errors.CodeError) {

	return
}

func scan(ctx context.Context, client Client, key string) (err errors.CodeError) {
	return
}
